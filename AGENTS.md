# AGENTS.md｜817 项目协作规则

本文件适用于所有接管本仓库的 AI。稳定产品事实以本文件和 `docs/MVP_DESIGN.md` 为准，不以历史聊天中的临时表述为准。

## 1. 开始工作前

1. 不要立即修改代码。
2. 完整阅读 `README.md`、本文件和 `docs/MVP_DESIGN.md`。
3. 检查 Git 状态、最近 5 次提交、目录结构和现有运行方式。
4. 复述产品定位、本期范围、已确认设计、本次允许修改内容和风险。
5. 未得到“理解正确，可以开始”前不得实施。

## 2. 产品范围

- 完整愿景是多功能 AI 商业化销售工作台，不得把最终产品误解为单一报告工具。
- 当前开发深度集中在投放复盘诊断 Agent；不得擅自增加行业早报、竞品监测、搜索词工具、素材生成、完整 CRM、自动投放等旁支。
- 完整蓝图与当前 MVP 必须分开表达。蓝图中的功能不等于本期必须开发。
- 未达到可用标准的模块不得以空页面占位。所有出现在产品导航中的页面至少应具备完整模拟数据、筛选和基本交互。

## 3. 单任务开发

- 每次只执行一个可验收任务。
- 修改前明确：目标、允许修改的文件、禁止修改的范围、数据影响、回归测试。
- 不得把“优化一下”“更高级”等开放描述理解为重新设计信息架构、数据模型或技术栈。
- 如发现必须修改已验收内容，先停止，说明原因、影响文件、迁移方式和替代方案，等待确认。
- 不得顺手重构、替换技术栈、删除功能或调整无关页面。

## 4. 产品与 AI 规则

- 第一版必须包含真正的 Agent 决策：数据完整性判断、分析路径、工具调用、异常下钻、案例检索、追问、复核和停止。
- 所有投放层级均须完成基础扫描和分层诊断，不能发现一个异常后跳过其他层级。
- 确定性指标由代码或规则工具计算，LLM 不负责关键指标心算。
- RAG 只负责召回可能相关的历史案例，不负责直接决定原因和建议。
- LLM 必须先基于当前客户数据形成初步诊断，再比较历史案例；不得用历史经验替代当前分析。
- 输出必须区分数据事实、合理推断和待验证假设。证据不足时追问、降级或停止，不得编造。
- 未经销售人工确认，不得把结果标记为标杆案例，也不得自动执行预算调整或广告启停。

## 5. 数据规则

- 真实产品的数据是每日由广告平台后台自动同步，不是销售手动输入。
- 本项目的模拟数据必须预先生成并持久化保存，通过模拟接口读取；不得每次诊断时由 LLM 临时随机生成。
- 数据生成需要固定随机种子、场景标签和预期异常，保证同一案例可重复评测。
- 页面不需要突出“同步数据”按钮，只在必要时显示“数据更新至某日”或数据缺失警告。
- 模拟数据、公开数据、脱敏历史材料必须在项目说明中披露来源边界，不能包装成企业真实数据。

## 6. 页面与交互规则

- MVP 不默认拆成四个相互孤立的页面。
- 核心体验以“客户投放复盘”主工作空间承载：客户和时间筛选、数据概览、全层级诊断、案例比较、报告预览与人工审核保持同一上下文。
- 历史案例库可以作为独立辅助入口。
- 数据同步属于后台能力，不设计独立同步页面。
- 网页报告与 PPT 使用同一份结构化报告数据，避免两套内容逻辑。

## 7. 验收与 Git

- 完成后只汇报：实际修改文件、预览方式、测试结果、对已验收功能的影响和建议验收步骤。
- 用户回复“验收通过”后再形成 Git 基线；未验收时不得擅自开始下一项。
- 每次提交只包含当前任务相关改动，提交信息需清晰说明结果。
- 默认远程仓库：`https://github.com/Averymeng/817`。
- 禁止破坏性回滚、覆盖或删除用户已有改动。

## 8. 表达与真实性边界

- 默认中文沟通，必要的 LLM、Agent、RAG、ROI、CTR 等专业术语可保留。
- 项目可以表述为“原型、模拟数据、离线评测、历史案例回放”。
- 无证据时不得写“企业上线”“真实用户”“自动接入小红书后台”“提升 ROI / 降低成本”等结果。

